const initState = {
  maxblood_upper: null,
  maxblood_lower: null,
  maxblood_upper_tuning: null,
  maxblood_middle_tuning: null,
  maxblood_lower_tuning: null,
  autoLogout: null,
  notesformat: null,
}
export default function (state = initState, action) {
  switch (action.type) {
    default:
      return state
  }
}
